/// Spacing scale tokens, in logical pixels.
///
/// PLACEHOLDER VALUES — a plain 8pt-ish scale. Replace the numbers once
/// real design tokens land; keep the same field names so widgets that
/// reference e.g. `AppSpacing.md` don't need to change.
abstract class AppSpacing {
  static const double xs = 4;
  static const double sm = 8;
  static const double md = 16;
  static const double lg = 24;
  static const double xl = 32;
  static const double xxl = 48;
}
