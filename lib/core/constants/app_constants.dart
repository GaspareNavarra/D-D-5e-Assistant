/// Cross-feature constants that don't belong to any single feature.
abstract class AppConstants {
  /// Point-buy budget for ability scores during character creation
  /// (D&D 5e standard rules).
  static const int pointBuyBudget = 27;
}
